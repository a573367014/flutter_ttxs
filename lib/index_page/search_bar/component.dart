import 'package:fish_redux/fish_redux.dart';
import 'package:flutter/material.dart';

class SearchBar extends Component {
  SearchBar() : super(
    view: (state, Dispatch dispatch, ViewService viewService) {
      return Container(
        child: Text('hahahah')
      );
    }
  );
}